# Copia este archivo a  .streamlit/secrets.toml  y rellena tus valores.
# NO subas secrets.toml al repositorio (ya está en .gitignore).
#
# Usa la clave SERVICE_ROLE (backend de confianza). Nunca la expongas
# en el navegador ni en el cliente.

SUPABASE_URL = "https://TU-PROYECTO.supabase.co"
SUPABASE_KEY = "eyJ...tu-service-role-key..."
